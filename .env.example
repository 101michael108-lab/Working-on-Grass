# LOCAL ONLY — copy to .env.local. Production uses apphosting.yaml + Firebase console.

NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin access: Firebase Auth UID(s), comma-separated (must match logged-in user)
ADMIN_UIDS=SRSc2O7T4XOnTe8ehJMl8oXLtwz2
NEXT_PUBLIC_ADMIN_UIDS=SRSc2O7T4XOnTe8ehJMl8oXLtwz2

# PayFast sandbox (local checkout always uses sandbox)
PAYFAST_PASSPHRASE=your-sandbox-passphrase
NEXT_PUBLIC_PAYFAST_SANDBOX_MERCHANT_ID=10043133
NEXT_PUBLIC_PAYFAST_SANDBOX_MERCHANT_KEY=wh7ky81lq556u

# Firebase Admin SDK — required for /admin login locally (generate key in Firebase Console)
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
# Or paste the full JSON on one line:
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account",...}
